{
  "name": "@community-platform/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"build configured in Phase 0.4 (tsup)\"",
    "lint": "echo \"lint configured in Phase 0.6\"",
    "test": "echo \"test configured in Phase 0.6\"",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  }
}
