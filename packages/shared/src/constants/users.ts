// ============================================================
// User Profile
// ============================================================

/**
 * 氏名カナ・ニックネームカナの入力許可文字パターン。
 * 全角カタカナ（ァ-ヶ）・長音符（ー）・全角/半角スペースのみ許可する。
 * 空文字も許可する（任意項目のため）。
 */
export const KANA_PATTERN = /^[ァ-ヶー　 ]*$/;
