generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// Models will be added starting in Phase 1
// See docs/テーブル定義書.md for the full 116-table schema
// Migration policy: one migration per phase (prisma migrate dev --name phaseN_description)
